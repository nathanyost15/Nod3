package com.node.sort;

import com.node.models.DirectedNode;

import java.util.*;

public class NodeStack extends Stack<DirectedNode> {
    public void pushAll(List<DirectedNode> directedNodes) {
        for(DirectedNode node : directedNodes) {
            this.push(node);
        }
    }
}
